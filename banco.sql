CREATE TABLE dados (
    company VARCHAR(255),
    cnpj VARCHAR(20),
    state VARCHAR(2),
    product VARCHAR(255),
    value DECIMAL(10, 2),
    dueDate DATE,
    date DATE,
    host VARCHAR(255)
);
